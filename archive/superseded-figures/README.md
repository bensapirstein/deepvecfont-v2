# Superseded figures

Kept for provenance, not used by the report.

- `bin_histogram_chn_train.png` — the day-1 quantization-grid diagnostic. It answered
  whether the training data sits on a 64-bin or 128-bin grid (§1.5 of `PROJECT_PLAN.md`),
  which is what kept candidate E13 on the list. The question was settled properly on
  2026-08-03 by reading `data_utils/relax_rep.py` rather than by reading this histogram,
  and the figure's own PARTIAL verdict came from a miscalibrated threshold. Superseded.
